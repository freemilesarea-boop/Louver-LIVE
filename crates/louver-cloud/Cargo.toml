[package]
name = "louver-cloud"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
louver-core = { path = "../louver-core" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
chrono.workspace = true
rusqlite.workspace = true
uuid.workspace = true
hex.workspace = true
base64.workspace = true
sysinfo.workspace = true
# Already in the tree via rustls. Used for sealing secrets (ChaCha20-Poly1305),
# hashing passwords (PBKDF2-HMAC-SHA256) and generating tokens, so the cloud
# adds no new crypto dependency and no new audit surface.

ring = "0.17"

[dev-dependencies]
tempfile.workspace = true
